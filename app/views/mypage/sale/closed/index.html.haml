= render 'joint/header'
= render 'users/user-breadcrumb', {symbol: :mypage_sale_closed_index}
.user-wrapper
  %main.user-main
    -# TODO: 販売済み商品をリスト表示する
    -# ナビゲーションバー
    = render 'users/user-navigation'
= render 'joint/app__dl'
= render 'joint/footer'
= render 'joint/exhibit__button'