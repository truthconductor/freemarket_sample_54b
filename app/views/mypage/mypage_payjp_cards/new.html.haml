= render 'joint/header'
= render 'users/user-breadcrumb', {symbol: :new_mypage_card}
.user-wrapper
  %main.user-main
    .card-container
      .card-container__header
        %h2.card-container__header__headline
          クレジットカード情報入力
      -# カード情報入力
      = render 'payjp_cards/form', {payjp_card: @payjp_card}
    -# ナビゲーションバー
    = render 'users/user-navigation'
= render 'joint/app__dl'
= render 'joint/footer'
= render 'joint/exhibit__button'