= render 'joint/header'
= render 'users/user-breadcrumb', {symbol: :mypage_purchase_closed_index}
.user-wrapper
  %main.user-main
    .user-container
      -# 購入完了した商品をリスト表示
      = render 'users/user-purchase-closed', {deals: @deals}
    -# ナビゲーションバー
    = render 'users/user-navigation'
= render 'joint/app__dl'
= render 'joint/footer'
= render 'joint/exhibit__button'